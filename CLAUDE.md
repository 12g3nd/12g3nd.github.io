# CLAUDE.md

@AGENTS.md

Everything lives in `AGENTS.md` — commands, invariants, and how to verify a
visual change. This file only imports it, so the two can never disagree. Codex
and other agents read `AGENTS.md` directly; add to that file, not this one.
